import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.first.ionic.project',
  appName: 'first-ionic-project',
  webDir: 'www',
};

export default config;
